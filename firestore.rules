rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Public reads; writes require a signed-in user with an email
    match /{document=**} {
      allow read: if true;
      // Allow writes for any authenticated user
      allow write: if request.auth != null;
    }
  }
}